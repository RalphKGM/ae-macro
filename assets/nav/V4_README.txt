Anime Expeditions — navigation detection templates
===================================================
Drop the cropped PNG templates here. The state machine looks for these EXACT filenames:

  shop_icon.png          - a stable lobby-only icon/button (Lobby detection)
  mm_leave.png           - LEAVE / back button on the play menu (Matchmaking screen)
  menu_play.png          - PLAY button on the mode panel (mode panel is open)
  autoplay_btn.png       - a STABLE always-present in-match HUD element (in a match)
  victory.png            - VICTORY banner text (win results)
  defeat.png             - DEFEAT banner text (loss results)
  reconnect_btn.png      - Roblox Reconnect button (Error 277 recovery)
  connection_failed.png  - Connection Failed popup (Error 279 recovery)

Capture with the game docked at 816x638 (press F6 in the macro, then F3 for a full-window
screenshot to _debug_screenshot.png, then crop each button tight). See ASSETS_NEEDED.md.
